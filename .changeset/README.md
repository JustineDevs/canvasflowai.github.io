# Changesets

Run `pnpm changeset` when a change should contribute release notes and version intent.

For this application repository, Changesets is used to keep human-authored release notes and
versioning metadata disciplined even though the project is not a public npm package.
